# 🔒 PRODUCTION SECURITY UPDATE: Latest stable versions for 100K users
# All packages updated to latest stable versions (2025-01-XX)

# Core Flask framework - Updated for security patches
Flask==3.0.3
gunicorn==22.0.0
Werkzeug==3.0.3
Jinja2==3.1.4
Flask-WTF==1.2.1
# Flask-Session==0.2.0  # DISABLED: Python 3.13互換性問題のため無効化

# Critical dependencies for Render.com deployment
psutil==5.9.8
redis==5.2.0
python-dotenv==1.1.0
pytz==2024.2

# Encoding support for Japanese CSV data
chardet==5.2.0

# Optional dependencies with fallback handling in code
# redis-py-cluster (handled with try/except)
# exam_simulator (handled with try/except)